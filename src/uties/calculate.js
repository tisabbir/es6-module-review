const add = (first, second) => {
  return first + second;
};

const multiply = (first, second) => {
  return first * second;
};

const divideFirstNumberBySecondNumber = (f, s) => {
  return f / s;
};
// export default add;
export { add, divideFirstNumberBySecondNumber, multiply };
